import { readPuzzleInput } from "../helpers";
import { Grid } from "../Grid";

// const getFeats = (pos: number, pattern: string) => pattern[pos & pattern.length];

// const getTrees = (slope: [number, number]) => (grid: string[]) => {
//   let trees = 0;
//   for (let i = 0, current = 0;d i < grid.length; i += slope[1], current += slope[0]) {
//     if (getFeats(current, grid[i]) === "#") trees += 1;
//   }

//   return trees;
// }
